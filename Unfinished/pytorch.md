### autograd
Training [[neural networks]] with [[gradient descent]] in PyTorch is made easy using the built-in **autograd engine**. PyTorch computes the [[gradients]] of a function with respect to the inputs by using automatic differentiation.
##### computation graph
https://pytorch.org/blog/computational-graphs-constructed-in-pytorch/
